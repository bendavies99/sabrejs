{
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run --parallel --no-bail test",
    "test:watch": "lerna run --parallel --no-bail test:watch",
    "run:dev": "lerna run --parallel run:dev",
    "run:watch": "lerna run --parallel run:watch",
    "build:dev": "lerna run build:dev",
    "build:prod": "lerna run --parallel build:prod"
  },
  "description": "SabreJS is a dependency injection framework using Typescript",
  "devDependencies": {
    "lerna": "3.22.1",
    "concurrently": "^7.1.0",
    "@types/node": "^18.8.5"
  },
  "author": "Ben Davies <me@bdavies.net>",
  "license": "MIT License"
}
